<!-- pdk:stub — remove this line once curated. Run /curate-manifest to fill this in. -->
# Usage rules

"Use X, not Y" knowledge that keeps prototypes on-system. These rules are read
by the prototyping skill before any code is written and by validation after.

## Component choices

<!-- prompt: Any "always use X for Y" rules? e.g. "Use Sheet for record detail,
     Dialog only for confirmations", "Never use raw <table>, always <Table>". -->

## Layout & spacing

<!-- prompt: Spacing scale discipline, container widths, grid conventions,
     forbidden ad-hoc margins? -->

## Color & tokens

<!-- prompt: Which semantic tokens must be used (and which raw values are
     forbidden)? Dark-mode expectations? -->

## Accessibility

<!-- prompt: Non-negotiables — focus states, label pairing, contrast, keyboard
     paths. -->

## Anti-patterns

<!-- prompt: Things reviewers keep rejecting. Be blunt; this section saves the
     most rework. -->
