# Areas

One folder per focused workstream (e.g. `framework/`, `forms/`, `data-tables/`).

Each area has a `brief.md` describing what's being explored, plus a `research/` folder for screenshots, references, and notes.

Empty by default — add areas as the work calls for them.
